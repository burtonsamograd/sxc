-- to compile *pointer++ you must enter it either literally as the
   symbol '*pointer++' or as '(*pointer ++)' or using the function
   (*?++ pointer).  Using the *?++ function is preferred so as to not
   need symbol creation in the code generating program.  Same for
   *pointer--.
