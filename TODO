-- handle lispy operations
    (+ a b c ...)

-- macros
   (macro (args ...) (code (CL)))

-- macros
   - design/sswitch.sxc for an example

-- add (inline-c "int x = 0;")

DONE -- add (struct name
     	  	    type1 field
		    type2 field ...)

-- Allow for nested struct definitions
   (struct point
   	   int x
	   int y
	   (struct data
	   	   int a
		   int b) d)