(#include <stdio.h>)

// count characters in input; 1st version
(int main ((int argc) (char (** argv)))
     (var long nc)

     (while (!= (getchar) EOF)
       (++ nc))
     (printf "%ld\n" nc)

     (return 0))
